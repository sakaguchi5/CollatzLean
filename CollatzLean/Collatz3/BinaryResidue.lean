import CollatzLean.Collatz3.Binary
import CollatzLean.Collatz3.Bridge.Residue

/-!
# Collatz3 BinaryResidue

B1 + B2 + R1 を一度に読む最上位集約 import。

* binary representation / alternating weight / defect
* deinterleave / carry / run resolution
* 3-adic residue channels / support
* actual `Runs` の residue lift

をまとめる。
-/
